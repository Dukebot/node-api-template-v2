const Config = {
    app: require('./app-config'),
    cors: require('./cors-config'),
    rateLimit: require('./rate-limit-config'),
    database: require('./database-config'),
}

module.exports = Config